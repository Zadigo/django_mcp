# Django/Python MCP 🐶

The Django MCP is a server can implement context protocol for Django applications. It provides tools to create, read, update, and delete 
resources in a standardized way. It also helps implement best practices for build complete applications using Django including packages that
work around the Django framework.
